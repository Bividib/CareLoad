import { RoutePlaceholder } from "@/components/shell/RoutePlaceholder";

export default function DemoPage() {
  return (
    <RoutePlaceholder
      title="Presenter controls"
      description="This route is reserved for deterministic synthetic checkpoints and reset controls. It is not patient navigation."
    />
  );
}

